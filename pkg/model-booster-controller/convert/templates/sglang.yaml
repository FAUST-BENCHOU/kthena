apiVersion: workload.serving.volcano.sh/v1alpha1
kind: ModelServing
metadata: ${MODEL_SERVING_TEMPLATE_METADATA}
spec:
  schedulerName: ${SCHEDULER_NAME}
  topologySpreadConstraints:
    - maxSkew: 1
      topologyKey: kubernetes.io/hostname
      whenUnsatisfiable: DoNotSchedule
      labelSelector:
        matchLabels:
          modelserving.volcano.sh/name: ${MODEL_NAME}
  replicas: ${BACKEND_REPLICAS}
  template:
    restartGracePeriodSeconds: 60
    gangPolicy:
      minRoleReplicas:
        leader: ${SERVER_REPLICAS}
    roles:
      - name: leader
        replicas: ${SERVER_REPLICAS}
        entryTemplate:
          metadata: ${SERVER_ENTRY_TEMPLATE_METADATA}
          spec:
            initContainers: ${INIT_CONTAINERS}
            terminationGracePeriodSeconds: 300
            volumes: ${VOLUMES}
            containers:
              - name: runtime
                image: ${MODEL_SERVING_RUNTIME_IMAGE}
                ports:
                  - containerPort: ${MODEL_SERVING_RUNTIME_PORT}
                env: ${ENGINE_ENV}
                envFrom: ${MODEL_DOWNLOAD_ENVFROM}
                args:
                  - --port
                  - ${MODEL_SERVING_RUNTIME_PORT}
                  - --engine
                  - ${MODEL_SERVING_RUNTIME_ENGINE}
                  - --engine-base-url
                  - ${MODEL_SERVING_RUNTIME_URL}
                  - --engine-metrics-path
                  - ${MODEL_SERVING_RUNTIME_METRICS_PATH}
                  - --pod
                  - ${MODEL_SERVING_RUNTIME_POD}
                  - --model
                  - ${MODEL_NAME}
                readinessProbe:
                  httpGet:
                    path: /health
                    port: ${MODEL_SERVING_RUNTIME_PORT}
                  initialDelaySeconds: 5
                  periodSeconds: 10
              - name: engine
                image: ${ENGINE_SERVER_IMAGE}
                command: ${ENGINE_SERVER_COMMAND}
                env: ${ENGINE_ENV}
                resources: ${ENGINE_SERVER_RESOURCES}
                volumeMounts: ${VOLUME_MOUNTS}
                readinessProbe:
                  failureThreshold: 3
                  httpGet:
                    path: /health
                    port: 30000
                    scheme: HTTP
                  initialDelaySeconds: 180
                  periodSeconds: 5
                  successThreshold: 1
                  timeoutSeconds: 1
                lifecycle:
                  preStop:
                    exec:
                      command:
                        - /bin/sh
                        - -c
                        - |
                          while true; do
                            QUEUE_REQS=$(curl -s http://localhost:30000/metrics | grep 'sglang:num_queue_reqs' | grep -v '#' | awk '{print $2}')
                            if [ -z "$QUEUE_REQS" ] || [ "$QUEUE_REQS" = "0.0" ]; then
                              echo "Terminating: No requests in queue, safe to terminate" >> /proc/1/fd/1
                              exit 0
                            else
                              echo "Terminating: Queue reqs: $QUEUE_REQS" >> /proc/1/fd/1
                              sleep 5
                            fi
                          done
        workerReplicas: ${WORKER_REPLICAS}
        workerTemplate:
          metadata: ${SERVER_WORKER_TEMPLATE_METADATA}
          spec:
            volumes: ${VOLUMES}
            initContainers: ${INIT_CONTAINERS}
            containers:
              - name: ${BACKEND_NAME}-${BACKEND_TYPE}-worker
                image: ${ENGINE_SERVER_IMAGE}
                command: ["true"]
                env: ${WORKER_ENV}
                resources: ${ENGINE_SERVER_RESOURCES}
                volumeMounts: ${VOLUME_MOUNTS}
